services:
  unraid-monitor-bot:
    build:
      context: .
      args:
        # Match the docker group GID on the host
        DOCKER_GID: ${DOCKER_GID:-999}
    container_name: unraid-monitor-bot
    restart: unless-stopped
    # Runs as appuser (UID 1000) in docker group - no root needed
    # Set DOCKER_GID to match your host's docker group: getent group docker | cut -d: -f3
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_ALLOWED_USERS=${TELEGRAM_ALLOWED_USERS}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - UNRAID_API_KEY=${UNRAID_API_KEY:-}
      - TZ=Europe/London
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # For development, use local directories:
      # - ./config:/app/config:ro
      # - ./data:/app/data
      # For production on Unraid, use bind mounts:
      - /mnt/user/appdata/unraid-monitor/config:/app/config
      - /mnt/user/appdata/unraid-monitor/data:/app/data
    networks:
      - docknet

networks:
  docknet:
    external: true
